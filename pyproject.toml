[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"

[project]
name = "home-ops"
version = "0.1.0"
description = "Real estate agentic pipeline: Idealista scraper, scoring engine, Telegram alerts"
requires-python = ">=3.11"
license = { text = "MIT" }

dependencies = [
    "scrapling[fetchers]>=0.4.15",
    "duckdb",
    "litellm",
    "python-telegram-bot",
    "typer",
    "rich",
    "pyyaml",
    "python-dotenv",
    "pytz>=2026.3.post1",
]

[project.optional-dependencies]
apify = ["apify-client"]
dev = [
    "pytest",
    "pytest-cov",
    "pytest-asyncio>=1.4.0",
    "ruff",
    "mypy",
    "pytest-env",
    "httpx>=0.28.1",
]
tui = [
    "textual>=8.2.8",
]
web = [
    "fastapi>=0.141.1",
    "jinja2>=3.1.6",
    "uvicorn[standard]>=0.52.4",
]

[project.urls]
repository = "https://github.com/AlejandroRS21/home-ops"

[project.scripts]
homeops = "home_ops.cli.app:app"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.11"
strict = true
ignore_missing_imports = true
disallow_untyped_defs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-v --cov=src/home_ops --cov-report=term-missing"

[tool.coverage.run]
source = ["src/home_ops"]

[tool.coverage.report]
fail_under = 70
